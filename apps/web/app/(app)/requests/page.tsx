import { Placeholder } from '@/components/Placeholder';

export default function RequestsPage() {
  return (
    <Placeholder
      title="Requests & reports"
      milestone="M6"
      scope="document matrix export, pre-junta and reviewer packs, manifest and data-room downloads, reproduce status, audit-log viewer, parameter and rule versions, sign-off."
    />
  );
}
