import { Placeholder } from '@/components/Placeholder';

export default function VendorsPage() {
  return (
    <Placeholder
      title="Vendors"
      milestone="M5"
      scope="vendor fact sheets from public registries, manual evidence uploads, possible links to verify (with expected homonym counts)."
    />
  );
}
