import { Placeholder } from '@/components/Placeholder';

export default function GroupingPage() {
  return (
    <Placeholder
      title="Grouping & duplicates"
      milestone="M2"
      scope="page-to-document grouping confirmation (merge / split / reorder / discard), near-duplicate review, mandatory confirmation for documents carrying 1,000 EUR or more."
    />
  );
}
