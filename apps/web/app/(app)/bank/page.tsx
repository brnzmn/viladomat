import { Placeholder } from '@/components/Placeholder';

export default function BankPage() {
  return (
    <Placeholder
      title="Bank & reconciliation"
      milestone="M3"
      scope="statement continuity strip, transaction flags, proposed links, residual sets R1–R6, derrama grid per unit and period."
    />
  );
}
