import { Placeholder } from '@/components/Placeholder';

export default function WorksPage() {
  return (
    <Placeholder
      title="Works packages"
      milestone="M4"
      scope="timeline per package, five-number triangulation (architect PEM, permit PEM, subsidy protegible, contract price, invoices), suspension status, price versus contract and approved budget."
    />
  );
}
